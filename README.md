# ArcGIS_Collector_Offline_Recovery
Convert a runtime (Collector) geodatabase to a file geodatabase and sync with an existing hosted feature service. Currently only supports username/password logins.
